# Atlas Stack Status
Generated by scaffolder. Read this before writing any code.

## Verdict: EXISTING — preserve current stack

## Detected stack
- Runtime: NestJS (TypeScript) — backend/; Angular (TypeScript) — web/frontend/
- ORM: Prisma (detected in backend/prisma/ and web/backend/prisma/)
- Auth: Custom JWT (template scaffold; finalize in backend/src/auth/)
- Health checks: MISSING — to be added at /api/health
- K8s service name: faithful-e2e-g (namespace: colossus-faithful-e2e-g)

## Rules for all agents
- Do not migrate the ORM
- Do not replace the auth implementation
- Do not change the framework
- Only add features within the existing patterns
- Angular frontend lives in web/frontend/; NestJS API lives in backend/
- Base-href is /faithful-e2e-g/ — do not change
- All API calls are relative to /api
